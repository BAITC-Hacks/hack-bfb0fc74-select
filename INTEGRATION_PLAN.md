# План интеграции Select — трек 06

Состояние на 23.09.2026: задачи Codex работают в общей ветке `main` (HEAD `6e881622`), без отдельных Git-diff. После UX/QA-аудита и точечных исправлений локально проходят 31 тест; финальные независимые перепроверки Build, QA и UI Preview ещё идут. На платформу решение пока не сдаём. Актуальная координация задач приведена в конце документа.

## Критерии и границы MVP

Вход: город, дата, формат мероприятия, категория, бюджет; опционально длительность и язык. Выход: до трёх карточек с именем, категорией, городом, начальной ценой и 1–2 конкретными предложениями объяснения. Занятые на дату исключаются, включая площадки. При 1–2 подходящих вариантах показать все и причины нехватки. Различать «подобрали», «категории нет в городе», «категория есть, но никто не проходит». Одинаковый запрос сохраняет порядок. Смена даты может менять тройку с видимым объяснением занятости. Живое демо: плотная, редкая и пустая категории; ответ ориентировочно до 10 секунд. Синтетические и заполненные при подготовке поля помечены.

За рамками: бронирование, заявки, уведомления, новые профили/поля запроса, векторный поиск и дообучение. Живой LLM необязателен после локального сквозного потока.

## Данные, стек и зависимости

`data/contractors.csv`: UTF-8 CSV, 66 записей, 13 полей: `id`, `anon_name`, `categories`, `city`, `city_imputed`, `synthetic`, `price_from_kzt`, `price_imputed`, `event_formats`, `languages`, `max_hours`, `busy_dates`, `description`. Списки разделены `|`; даты — ISO `YYYY-MM-DD`, календарь 23.09–31.12.2026. Девять пустых `max_hours` означают «не применимо». 13 профилей помечены `synthetic`. Цена — «от» за мероприятие. Образец синтетических профилей и расхождения описаний со структурированными полями описаны в `HACKATHON_CONTEXT.md`.

Стек: Python 3.11+, один процесс Streamlit для быстро воспроизводимого локального интерфейса; стандартные `csv` и `dataclasses` для каталога и контракта; OpenAI SDK только для необязательного выбора проверяемых фрагментов описания; pytest только для разработки. Новые зависимости добавляются лишь при блокирующей необходимости. Базовый запуск требует только `requirements.txt`; необязательные AI-зависимости вынесены в `requirements-ai.txt`, проверки — в `requirements-dev.txt`.

## Актуальный контракт Frontend ↔ Backend

Обязательный путь: `models.Request(city, date, event_format, category, budget_kzt, duration_hours=None, language=None)` → `matcher.match(profiles, request)` → `models.MatchResult` → `explanations.build_explanations(result, request, use_ai=False)` → карточки Streamlit. `Profile` хранит 13 полей CSV: `id`, `anon_name`, `categories`, `city`, `city_imputed`, `synthetic`, `price_from_kzt`, `price_imputed`, `event_formats`, `languages`, `max_hours`, `busy_dates`, `description`. Списки в файле разделены `|`, даты имеют ISO-формат; в Python списки — кортежи, даты — `frozenset[date]`, флаги — bool, `max_hours` — `int | None`.

`MatchResult` содержит `status` (`matched`, `no_category`, `no_eligible`), `cards` (не более трёх `Profile` в окончательном порядке), `eligible_count`, `category_count`, `exclusion_counts` по ключам `busy`, `budget`, `format`, `language`, `duration`. `build_explanations` возвращает `(dict[id, text], mode)`, где `mode` — `local`, `ai` или `fallback`, без изменения состава и порядка. Backend добавил необязательное `build_match_reasons(result, request)`: для выбранных карточек оно возвращает проверки полей CSV с `field`, `operator`, `actual`, `requested`. Frontend сейчас его не вызывает. Общие сигнатуры, правила фильтрации и структуру CSV меняет только интегратор после согласования; Frontend и Backend могут продолжать параллельно в своих файлах.

## Отдельные задачи Codex и владение

Все пять задач используют `/Users/andnight/Documents/hack-bfb0fc74-select` и общую `main`. Отдельных веток и отдельных Git-diff на задачу нет; результаты и владение подтверждены их отчётами и общим diff. Итоговые отчёты получены; Build, UI Preview и QA работают только в режиме проверки, пока интегратор не назначит конкретное исправление.

| Задача / роль | Изменяемые файлы | Готовый результат | Блокер и следующий шаг |
| --- | --- | --- | --- |
| `Frontend-разработчик` | Только `app.py` | Форма, до трёх карточек, дата, загрузка, ошибки, малый/пустой результат, пометки происхождения данных | Блокеров нет; брать лишь приоритетные замечания UI |
| `Backend / логика подбора` | Только `matcher.py`, `tests/test_matcher.py` | Строгий стабильный отбор и дополнительные структурированные причины по CSV | Блокеров нет; не менять `match` и UI; вернуться при дефекте фильтрации |
| `Build / интеграция` | Пока без правок; конкретный блокер запуска после назначения | `compileall`, 29 тестов и HTTP 200 | Блокеров нет; повторить запуск после очередной интеграции |
| `UI Preview / визуальный обзор` | Без правок на проверке | Пять замечаний по читаемости формы и выдачи | Приоритизация у интегратора; исправления назначать владельцу `app.py` |
| `QA / проверка требований` | Без правок на проверке | Обязательные сценарии прошли; один P3 по слабой цитате | После исправления перепроверить затронутый сценарий |
| Основной интегратор | `models.py`, `README.md`, `HACKATHON_CONTEXT.md`, `MVP_CONTRACT.md`, `INTEGRATION_PLAN.md`, `tests/test_mvp_flow.py`, конфиги, зависимости, исходный CSV | Общий контракт, интеграция и приёмка | Не менять данные и контракт без подтверждённой причины |

`explanations.py` и `tests/test_explanations.py` пока закреплены за интегратором. Если исправлять P3, назначить одному владельцу только эти файлы. Одновременное редактирование одного файла разными задачами запрещено.

## Текущая очередь интеграции

1. Базовый сквозной сценарий уже собран: CSV → `match` → локальное объяснение → Streamlit. Frontend и Backend работали параллельно в отдельных файлах без изменения обязательного контракта.
2. Build подключился после этого сценария: компиляция, 29 тестов и живой Streamlit с HTTP 200. Сервер после проверки остановлен; запрет песочницы на открытие порта не является дефектом приложения.
3. UI Preview и QA после запуска проверили приложение без правок. QA не нашёл P0–P2. P3: для HK-25279, Алматы / 24.09.2026 / корпоратив / Лайв-бэнд / 800 000 ₸ цитата сообщает только название группы. UI Preview предложил поднять пояснение смены даты над карточками, компактнее показать тройку, объяснить порядок, скрывать или помечать старую выдачу после изменения формы, заметнее пометить синтетику и заполненные поля.
4. Приоритет: блокер запуска → нарушение обязательного критерия → P3 и читаемость. Каждую правку получает один владелец файла; после небольшой порции интегратор повторяет затронутые тесты и сквозной сценарий, Build проверяет запуск, QA/UI Preview повторно смотрят только исправленные места.
5. Сохранить резерв на финальный запуск на машине участника и демонстрацию плотного, редкого, пустого результата и смены даты. Новые функции не добавлять. Живой необязательный AI-режим без ключа пока не проверен.

## UX/QA-аудит и владение последней интеграцией

Отдельные задачи провели чтение без правок: Frontend — UX, клавиатура и 360 px; UI Preview — компоновка wide/360 px и оба дизайн-референса; QA — ручные сценарии и fallback; Backend — сверка результата с CSV; Build — запуск, тесты и логи. Первый референс используется только для стиля тонких контурных иконок, второй — для структуры страницы.

Подтверждённый P1: в превью города часть SVG отображалась исходным кодом. Frontend — единственный владелец `app.py` — устранил этот вывод, сделал семантические заголовки и отдельные строки формы с подписями слева на широком экране; название селектора учитывает «Зарубежье». Смежная существующая задача «Исследуй хакатон HackAlem AI» владеет только `assets/cities/*.png` и `.streamlit/config.toml`: её изображения Алматы/Астаны и светлая тема сохранены; запись в `app.py` ею остановлена. Эти файлы нужно включить при передаче проекта на запуск.

Backend исправил только несогласованность дополнительной `build_match_reasons` при `language=''` в `matcher.py` и `tests/test_matcher.py`, сохранив `match()`. Интегратор исправил P3 — пустую цитату с одним названием группы — в `explanations.py` и `tests/test_explanations.py`. После этих изменений локально проходят 31 тест; финальные Build/QA/UI Preview повторяют проверку только чтением. Hover-preview пункта в нативном Streamlit select не поддерживается: превью меняется после подтверждения выбора мышью или клавиатурой. Реальное touch-событие инструментами проверки не эмулировалось.
